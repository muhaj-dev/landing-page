import { Notifications } from '@/containers/notifications/Notifications'

export const metadata = {
  title: "Notifications",
};

const page = () => {
  return (
    <>
    <Notifications />
    </>
  )
}

export default page