import TermsCondition from "@/containers/terms/TermsCondition";

export const metadata = {
  title: "Terms",
};

const page = () => {
  return (
    <div>
      <TermsCondition />
    </div>
  );
};

export default page;
