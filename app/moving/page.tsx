import { Moving } from "@/containers/MovingPage/Moving";

export const metadata = {
  title: "Moving",
};

const page = () => {
  return (
    <div>
      <Moving />
    </div>
  );
};

export default page;
