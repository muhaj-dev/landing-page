import AboutUs from "@/containers/aboutus/AboutUs";

export const metadata = {
  title: "About",
};

const page = () => {
  return (
    <div>
      <AboutUs />
    </div>
  );
};

export default page;
