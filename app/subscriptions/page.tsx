import Subscriptions from "@/containers/subscriptions/Subscriptions";

export const metadata = {
  title: "Subscriptions",
};

const page = () => {
  return (
    <div>
      <Subscriptions />
    </div>
  );
};

export default page;
